apiVersion: synthesis/v1
kind: git.pull-request
metadata:
  name: pr-fix-mcp-auth
spec:
  displayName: "fix: MCP session auth header forwarding"
  repoRef: "!name synthesis-backend"
  sourceBranch: fix/mcp-auth-header
  targetBranch: main
  prNumber: 15
  description: "Fixes OIDC token forwarding in MCP streamable HTTP sessions."
  mergeStatus: open
  reviewStatus: approved
  lifecycleState: review
  additions: 23
  deletions: 8
  changedFiles: 3
