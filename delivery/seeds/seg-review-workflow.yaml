apiVersion: synthesis/v1
kind: delivery.segment
metadata:
  name: seg-review-workflow
spec:
  displayName: "MCP Change Review"
  description: "Change approval workflow for domain write operations — pending change queue, per-change diffs, approve/reject with comments."
  valueStreamRef: "!name vs-domain-management"
  fundingModel: percent
  fundingValue: 20
  opsInScope: false
