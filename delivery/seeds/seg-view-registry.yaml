apiVersion: synthesis/v1
kind: delivery.segment
metadata:
  name: seg-view-registry
spec:
  displayName: "View Type Registry"
  description: "Extensible layout type registry dispatching to built-in React views or web component bridges. New view types plug in without modifying core code."
  valueStreamRef: "!name vs-architecture-viz"
  fundingModel: percent
  fundingValue: 20
  opsInScope: false
