apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-cancel-run
spec:
  displayName: "cancel_run"
  endpointType: mcp-tool
  toolName: "cancel_run"
  description: "Cancel an in-progress execution run (deprecated, use notebook_cancel_run)."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
  lifecycleState: deprecated
