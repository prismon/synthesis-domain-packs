apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-chat-sandbox-session
spec:
  displayName: "chat_sandbox_session"
  endpointType: mcp-tool
  toolName: "chat_sandbox_session"
  description: "Send a chat message to a sandbox session and await a response."
  permissionRequired: tenant_user
  componentRef: "!name ss-sandbox-orchestrator"
