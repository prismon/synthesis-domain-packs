apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-domain-delete-definition
spec:
  displayName: "Domain Delete Definition"
  endpointType: mcp-tool
  toolName: "delete_definition"
  parameters:
    required: [uid]
    optional: []
  description: "Deletes a domain definition."
  permissionRequired: tenant_admin
  componentRef: "!name ss-mcp-server"
