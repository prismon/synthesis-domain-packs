apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-proxy-mcp-call
spec:
  displayName: "proxy_mcp_call"
  endpointType: mcp-tool
  toolName: "proxy_mcp_call"
  description: "Proxy an MCP tool call to a registered external MCP server."
  permissionRequired: tenant_admin
  componentRef: "!name ss-mcp-server"
