apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-query-graph
spec:
  displayName: "query_graph"
  endpointType: mcp-tool
  toolName: "query_graph"
  description: "Traverse the domain graph starting from an item in a given direction."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
