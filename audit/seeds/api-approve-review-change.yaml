apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-approve-review-change
spec:
  displayName: "approve_review_change"
  endpointType: mcp-tool
  toolName: "approve_review_change"
  description: "Approve a single pending review change."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
