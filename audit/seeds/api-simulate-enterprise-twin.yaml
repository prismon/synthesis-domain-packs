apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-simulate-enterprise-twin
spec:
  displayName: "simulate_enterprise_twin"
  endpointType: mcp-tool
  toolName: "simulate_enterprise_twin"
  description: "Run a simulation scenario against an enterprise digital twin."
  permissionRequired: tenant_user
  componentRef: "!name ss-enterprise-engine"
  buildTag: enterprise
