apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-detach-item
spec:
  displayName: "detach_item"
  endpointType: mcp-tool
  toolName: "detach_item"
  description: "Detach an item from its parent in a hierarchical structure."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
