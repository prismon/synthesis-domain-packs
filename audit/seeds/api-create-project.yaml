apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-create-project
spec:
  displayName: "create_project"
  endpointType: mcp-tool
  toolName: "create_project"
  description: "Create a new project within the current tenant."
  permissionRequired: tenant_admin
  componentRef: "!name ss-mcp-server"
