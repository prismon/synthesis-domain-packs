apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-approve-changeset-item
spec:
  displayName: "approve_changeset_item"
  endpointType: mcp-tool
  toolName: "approve_changeset_item"
  description: "Approve a single item within a changeset."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
