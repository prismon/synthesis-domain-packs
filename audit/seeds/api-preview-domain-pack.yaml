apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-preview-domain-pack
spec:
  displayName: "preview_domain_pack"
  endpointType: mcp-tool
  toolName: "preview_domain_pack"
  description: "Preview what a domain pack would create before loading it."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
