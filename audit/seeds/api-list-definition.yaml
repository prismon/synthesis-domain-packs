apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-list-definition
spec:
  displayName: "list_definition"
  endpointType: mcp-tool
  toolName: "list_definition"
  description: "List all domain definitions registered in the system."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
