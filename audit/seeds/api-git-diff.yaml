apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-git-diff
spec:
  displayName: "git_diff"
  endpointType: mcp-tool
  toolName: "git_diff"
  description: "View the diff of uncommitted changes in a repository item."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
