apiVersion: synthesis/v1
kind: tech-stack.blueprint
metadata:
  name: ts-trait-composition
spec:
  displayName: "Trait Composition"
  useCaseRef: "!name uc-synthesis-blueprints"
  ring: Invest
  patternType: architectural
  openSourceAdoption: internal
  strengths:
    - "Reusable behavioral field injection"
    - "Schema composition without inheritance"
  description: "Domain definitions compose traits (Ownable, Trackable, etc.) that inject fields"
