apiVersion: synthesis/v1
kind: system.view
metadata:
  name: ticket-table-view
spec:
  displayName: "Tickets Table"
  description: "Tabular list of all tickets"
  itemGroup:
    kinds:
      - tasks.ticket
  viewComponentRef: "!name vc-table"
