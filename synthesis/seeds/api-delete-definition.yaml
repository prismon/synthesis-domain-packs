apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-delete-definition
spec:
  displayName: "delete_definition"
  endpointType: mcp-tool
  toolName: "delete_definition"
  description: "Delete a domain definition by UID."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
