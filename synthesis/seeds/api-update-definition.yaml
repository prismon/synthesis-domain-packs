apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-update-definition
spec:
  displayName: "update_definition"
  endpointType: mcp-tool
  toolName: "update_definition"
  description: "Update an existing domain definition with optimistic concurrency."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
