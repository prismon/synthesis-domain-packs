apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-validate-synthesis-branch
spec:
  displayName: "validate_synthesis_branch"
  endpointType: mcp-tool
  toolName: "validate_synthesis_branch"
  description: "Validate a synthesis domain branch for correctness and consistency."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
