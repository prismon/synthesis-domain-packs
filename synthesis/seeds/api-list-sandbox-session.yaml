apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-list-sandbox-session
spec:
  displayName: "list_sandbox_session"
  endpointType: mcp-tool
  toolName: "list_sandbox_session"
  description: "List all sandbox sessions for the current tenant."
  permissionRequired: tenant_user
  componentRef: "!name ss-sandbox-orchestrator"
