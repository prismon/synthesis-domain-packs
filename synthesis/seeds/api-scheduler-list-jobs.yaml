apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-scheduler-list-jobs
spec:
  displayName: "scheduler_list_jobs"
  endpointType: mcp-tool
  toolName: "scheduler_list_jobs"
  description: "List all scheduled jobs for the current tenant."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
