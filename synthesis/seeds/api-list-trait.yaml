apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-list-trait
spec:
  displayName: "list_trait"
  endpointType: mcp-tool
  toolName: "list_trait"
  description: "List all trait definitions registered in the system."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
