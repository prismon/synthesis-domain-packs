apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-scheduler-trigger-job
spec:
  displayName: "scheduler_trigger_job"
  endpointType: mcp-tool
  toolName: "scheduler_trigger_job"
  description: "Manually trigger a scheduled job outside its cron schedule."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
