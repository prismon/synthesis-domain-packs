apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-dispose-thread
spec:
  displayName: "dispose_thread"
  endpointType: mcp-tool
  toolName: "dispose_thread"
  description: "Dispose of a thread and release its resources."
  permissionRequired: tenant_user
  componentRef: "!name ss-sandbox-orchestrator"
