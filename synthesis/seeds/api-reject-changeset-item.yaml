apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-reject-changeset-item
spec:
  displayName: "reject_changeset_item"
  endpointType: mcp-tool
  toolName: "reject_changeset_item"
  description: "Reject a single item within a changeset."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
