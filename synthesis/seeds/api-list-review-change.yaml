apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-list-review-change
spec:
  displayName: "list_review_change"
  endpointType: mcp-tool
  toolName: "list_review_change"
  description: "List pending review changes for the current tenant."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
