apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-notebook-run
spec:
  displayName: "notebook_run"
  endpointType: mcp-tool
  toolName: "notebook_run"
  description: "Execute a notebook item and return a run UID."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
