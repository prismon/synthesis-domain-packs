apiVersion: synthesis/v1
kind: audit.api-endpoint
metadata:
  name: api-generate-synthesis-context
spec:
  displayName: "generate_synthesis_context"
  endpointType: mcp-tool
  toolName: "generate_synthesis_context"
  description: "Generate a rich context document describing the current platform state."
  permissionRequired: tenant_user
  componentRef: "!name ss-mcp-server"
